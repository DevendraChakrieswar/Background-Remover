# Example Spring Boot configuration
# Copy to application.properties and fill in real values (do NOT commit secrets)

server.port=8080

spring.application.name=bg-remover

# Datasource (uncomment and configure if used)
# spring.datasource.url=jdbc:postgresql://localhost:5432/bgremover
# spring.datasource.username=your_username
# spring.datasource.password=your_password
# spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=false

# Logging
# logging.level.root=INFO
# logging.file.name=logs/application.log

# Security/Secrets (example placeholders)
# removebg.api.key=YOUR_API_KEY_HERE
